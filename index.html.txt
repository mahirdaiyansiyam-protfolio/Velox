<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
    <title>VELOX — AI Model Studio | Royalty Visual Agency</title>
    <!-- Google Fonts: Royalty pairing -->
    <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;500;600;700;900&family=Cormorant+Garamond:ital,wght@0,400;0,500;0,600;1,400;1,500&family=Lora:wght@400;500;600&family=Jost:wght@300;400;500;600&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background-color: #0A0715;
            color: #FCFAF7;
            font-family: 'Lora', serif;
            overflow-x: hidden;
            cursor: default;
        }

        /* Smooth scroll */
        html {
            scroll-behavior: smooth;
            overflow-x: hidden;
        }

        /* ----- ROYALTY COLOR SYSTEM ----- */
        :root {
            --bg-primary: #0A0715;
            --bg-secondary: #130F1F;
            --bg-tertiary: #1A142D;
            --text-primary: #FCFAF7;
            --text-muted: #A99FBF;
            --gold: #D4AF37;
            --gold-light: #F5E7C8;
            --border-gold: rgba(212, 175, 55, 0.25);
            --transition-smooth: all 0.4s cubic-bezier(0.2, 0.9, 0.4, 1.1);
        }

        /* Typography */
        h1, h2, h3, .heading-font, .stat-number {
            font-family: 'Cinzel', serif;
            font-weight: 500;
        }

        .italic-luxury {
            font-family: 'Cormorant Garamond', serif;
            font-style: italic;
        }

        .section-label {
            font-family: 'Jost', sans-serif;
            font-size: 0.7rem;
            letter-spacing: 0.2em;
            text-transform: uppercase;
            color: var(--gold);
            margin-bottom: 1rem;
            display: inline-block;
        }

        /* Layout utility */
        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 4rem;
        }

        section {
            padding: 110px 0;
            position: relative;
            z-index: 2;
        }

        /* Navbar */
        nav {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            padding: 1.4rem 4rem;
            background: rgba(10, 7, 21, 0.85);
            backdrop-filter: blur(16px);
            z-index: 1000;
            transition: padding 0.3s ease;
            border-bottom: 1px solid var(--border-gold);
        }
        nav.shrink {
            padding: 0.8rem 4rem;
        }
        .nav-inner {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .logo {
            font-family: 'Cinzel', serif;
            font-size: 1.8rem;
            font-weight: 700;
            letter-spacing: 1px;
        }
        .logo span {
            color: var(--gold);
        }
        .nav-links {
            display: flex;
            gap: 2.5rem;
        }
        .nav-links a {
            font-family: 'Jost', sans-serif;
            font-size: 0.72rem;
            text-transform: uppercase;
            letter-spacing: 0.12em;
            color: var(--text-muted);
            text-decoration: none;
            transition: color 0.3s;
        }
        .nav-links a:hover {
            color: var(--gold);
        }
        .btn-sample {
            background: var(--gold);
            color: #0A0715 !important;
            padding: 0.6rem 1.4rem;
            border-radius: 0px;
            font-family: 'Jost', sans-serif;
            font-weight: 600;
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            transition: var(--transition-smooth);
            border: none;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
        }
        .btn-sample:hover {
            background: #b38f2c;
            transform: scale(0.97);
        }
        .mobile-menu-btn {
            display: none;
        }

        /* Hero */
        .hero {
            min-height: 100vh;
            position: relative;
            display: flex;
            align-items: flex-end;
            padding-bottom: 6rem;
            z-index: 2;
        }
        .hero-content {
            position: relative;
            z-index: 3;
            max-width: 800px;
        }
        .hero-tag {
            font-family: 'Jost', sans-serif;
            font-size: 0.7rem;
            letter-spacing: 0.2em;
            color: var(--gold);
            text-transform: uppercase;
            margin-bottom: 1.2rem;
            opacity: 0;
            transform: translateY(36px);
            transition: opacity 0.75s ease, transform 0.75s ease;
        }
        .hero-tag.in {
            opacity: 1;
            transform: translateY(0);
        }
        .hero-headline {
            font-family: 'Cinzel', serif;
            font-size: clamp(3.2rem, 7.5vw, 6.5rem);
            font-weight: 700;
            line-height: 1.02;
            margin-bottom: 2rem;
            opacity: 0;
            transform: translateY(36px);
            transition: opacity 0.75s ease 0.1s, transform 0.75s ease 0.1s;
        }
        .hero-headline.in {
            opacity: 1;
            transform: translateY(0);
        }
        .hero-headline i {
            font-family: 'Cormorant Garamond', serif;
            font-style: italic;
            color: var(--gold);
        }
        .hero-bottom {
            display: flex;
            justify-content: space-between;
            align-items: flex-end;
            flex-wrap: wrap;
            gap: 2rem;
            margin-top: 2rem;
            opacity: 0;
            transform: translateY(36px);
            transition: opacity 0.75s ease 0.2s, transform 0.75s ease 0.2s;
        }
        .hero-bottom.in {
            opacity: 1;
            transform: translateY(0);
        }
        .stats {
            display: flex;
            gap: 2.5rem;
        }
        .stat-item {
            text-align: center;
        }
        .stat-number {
            font-size: 2.2rem;
            font-weight: 700;
            color: var(--gold);
            font-family: 'Cinzel', serif;
        }
        .stat-label {
            font-family: 'Jost', sans-serif;
            font-size: 0.65rem;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            color: var(--text-muted);
        }
        .scroll-hint {
            position: absolute;
            bottom: 2rem;
            left: 50%;
            transform: translateX(-50%);
            text-align: center;
            font-size: 0.7rem;
            letter-spacing: 0.2em;
            color: var(--text-muted);
            z-index: 3;
        }
        .scroll-line {
            width: 1px;
            height: 40px;
            background: var(--gold);
            margin: 0.5rem auto 0;
            animation: pulse 1.8s infinite;
        }
        @keyframes pulse {
            0% { opacity: 0.3; height: 20px; }
            50% { opacity: 1; height: 40px; }
            100% { opacity: 0.3; height: 20px; }
        }

        /* Reveal animations */
        .rv, .rv-left, .rv-right {
            opacity: 0;
            transition: opacity 0.7s ease, transform 0.7s ease;
        }
        .rv { transform: translateY(36px); }
        .rv-left { transform: translateX(-40px); }
        .rv-right { transform: translateX(40px); }
        .rv.in, .rv-left.in, .rv-right.in {
            opacity: 1;
            transform: translate(0, 0);
        }

        /* Cards + grids */
        .problem-grid, .services-grid, .why-grid, .offerings-grid {
            display: grid;
            gap: 1px;
            background: var(--border-gold);
            margin-top: 3rem;
        }
        .problem-grid {
            grid-template-columns: repeat(4, 1fr);
        }
        .p-card, .svc-card, .why-card {
            background: var(--bg-secondary);
            padding: 2rem 1.5rem;
            transition: var(--transition-smooth);
            position: relative;
            transform-style: preserve-3d;
        }
        .p-card:hover, .svc-card:hover {
            transform: perspective(700px) rotateY(0deg) rotateX(0deg) scale(1.02);
            z-index: 5;
        }
        .card-number {
            font-size: 3rem;
            font-family: 'Cinzel', serif;
            color: rgba(212, 175, 55, 0.25);
            line-height: 1;
        }
        .p-card h3, .svc-card h3 {
            font-family: 'Cinzel', serif;
            margin: 1rem 0 0.5rem;
        }
        /* bottom sweep */
        .p-card::after, .svc-card::after, .why-card::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 2px;
            background: var(--gold);
            transform: scaleX(0);
            transform-origin: left;
            transition: transform 0.4s ease;
        }
        .p-card:hover::after, .svc-card:hover::after, .why-card:hover::after {
            transform: scaleX(1);
        }

        .services-grid {
            grid-template-columns: repeat(3, 1fr);
        }
        .svc-card {
            background: var(--bg-tertiary);
            position: relative;
            overflow: hidden;
        }
        .svc-card svg {
            stroke: var(--gold);
            margin-bottom: 1rem;
        }

        /* Tabs Showcase */
        .showcase-tabs {
            display: flex;
            gap: 1rem;
            margin: 2rem 0;
            border-bottom: 1px solid var(--border-gold);
            padding-bottom: 0.5rem;
        }
        .tab-btn {
            background: transparent;
            border: none;
            padding: 0.6rem 1.2rem;
            font-family: 'Jost', sans-serif;
            text-transform: uppercase;
            font-size: 0.7rem;
            letter-spacing: 0.1em;
            color: var(--text-muted);
            cursor: pointer;
            transition: all 0.3s;
        }
        .tab-btn.active {
            background: var(--gold);
            color: #0A0715;
            font-weight: 600;
        }
        .tab-content {
            display: none;
        }
        .tab-content.active-tab {
            display: block;
        }
        .model-grid {
            display: grid;
            gap: 2px;
            background: var(--border-gold);
        }
        .slot {
            background: #1A142D;
            aspect-ratio: 3/4;
            position: relative;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            text-align: center;
            transition: 0.2s;
            cursor: default;
            font-family: 'Jost', sans-serif;
            color: var(--text-muted);
            font-size: 0.75rem;
        }
        .slot span.coming {
            font-size: 0.7rem;
            letter-spacing: 1px;
            border: 1px solid var(--gold);
            padding: 0.2rem 0.8rem;
            border-radius: 20px;
            background: rgba(0,0,0,0.5);
        }
        .slot .slot-number {
            position: absolute;
            top: 12px;
            left: 12px;
            font-family: 'Cinzel', serif;
            color: var(--gold);
            opacity: 0.5;
        }
        .slot .slot-label {
            position: absolute;
            bottom: 12px;
            left: 0;
            right: 0;
            text-align: center;
            font-size: 0.65rem;
            text-transform: uppercase;
            background: rgba(0,0,0,0.6);
            padding: 5px;
            opacity: 0;
            transition: 0.2s;
        }
        .slot:hover .slot-label {
            opacity: 1;
        }
        /* 6x4 grid */
        .grid-6x4 {
            grid-template-columns: repeat(6, 1fr);
        }
        .grid-6x2 {
            grid-template-columns: repeat(6, 1fr);
        }
        .grid-4x2 {
            grid-template-columns: repeat(4, 1fr);
        }
        .sample-slot {
            aspect-ratio: 1/1;
            background: #130F1F;
            border: 1px solid var(--gold);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            position: relative;
        }
        .sample-badge {
            position: absolute;
            top: 8px;
            right: 8px;
            font-size: 0.55rem;
            background: var(--gold);
            color: #0A0715;
            padding: 2px 8px;
        }

        /* Process sticky */
        .process-wrapper {
            display: flex;
            gap: 4rem;
        }
        .process-sticky {
            position: sticky;
            top: 120px;
            height: fit-content;
            flex: 1;
        }
        .process-steps {
            flex: 1;
        }
        .step-row {
            padding: 2rem 0;
            border-top: 1px solid var(--border-gold);
        }
        .offerings-list {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1px;
            background: var(--border-gold);
        }
        .offering-item {
            background: var(--bg-primary);
            padding: 1.2rem;
            display: flex;
            justify-content: space-between;
        }

        /* CTA Light */
        .cta-light {
            background: #FCFAF7;
            color: #0A0715;
        }
        .cta-card {
            background: #0A0715;
            padding: 2rem;
            border-radius: 0px;
        }

        footer {
            background: #040205;
            padding: 3rem 0;
            text-align: center;
        }

        @media (max-width: 900px) {
            .container {
                padding: 0 1.5rem;
            }
            section {
                padding: 70px 0;
            }
            nav {
                padding: 1rem 1.5rem;
            }
            nav.shrink {
                padding: 0.6rem 1.5rem;
            }
            .nav-links {
                display: none;
            }
            .problem-grid {
                grid-template-columns: repeat(2, 1fr);
            }
            .services-grid {
                grid-template-columns: 1fr;
            }
            .process-wrapper {
                flex-direction: column;
            }
            .process-sticky {
                position: static;
            }
            .offerings-list {
                grid-template-columns: 1fr;
            }
            .why-grid {
                grid-template-columns: 1fr;
            }
            .hero-bottom {
                flex-direction: column;
                align-items: flex-start;
            }
            .grid-6x4, .grid-6x2 {
                grid-template-columns: repeat(3, 1fr);
            }
            .grid-4x2 {
                grid-template-columns: repeat(2, 1fr);
            }
            .custom-cursor, .cursor-ring {
                display: none;
            }
            body {
                cursor: auto;
            }
        }

        /* Custom cursor */
        .custom-cursor {
            width: 12px;
            height: 12px;
            background: var(--gold);
            border-radius: 50%;
            position: fixed;
            pointer-events: none;
            z-index: 10000;
            mix-blend-mode: difference;
            transform: translate(-50%, -50%);
            transition: width 0.2s, height 0.2s;
        }
        .cursor-ring {
            width: 40px;
            height: 40px;
            border: 1.5px solid var(--gold);
            border-radius: 50%;
            position: fixed;
            pointer-events: none;
            z-index: 9999;
            transform: translate(-50%, -50%);
            transition: width 0.2s, height 0.2s, border 0.2s;
        }
    </style>
</head>
<body>

<nav id="navbar">
    <div class="nav-inner container" style="padding:0">
        <div class="logo">VELOX<span>.</span></div>
        <div class="nav-links">
            <a href="#services">Services</a>
            <a href="#showcase">Showcase</a>
            <a href="#process">Process</a>
            <a href="#offerings">Offerings</a>
        </div>
        <a href="https://wa.me/8801790330221?text=Hi%2C%20I'd%20like%20a%20free%20sample%20image%20for%20my%20product" target="_blank" class="btn-sample">Free Sample</a>
    </div>
</nav>

<!-- Hero -->
<section class="hero" id="hero">
    <div class="container hero-content">
        <div class="hero-tag">AI Model Photography · Bangladesh</div>
        <div class="hero-headline">Your product.<br>A professional model.<br><i>No photoshoot.</i></div>
        <div class="hero-bottom">
            <div class="hero-desc" style="max-width:380px">Transform any product image into catalog-ready fashion visuals with AI models. Choose everything — model, background, pose.</div>
            <div class="stats">
                <div class="stat-item"><div class="stat-number">48h</div><div class="stat-label">Delivery</div></div>
                <div class="stat-item"><div class="stat-number">100%</div><div class="stat-label">Custom</div></div>
                <div class="stat-item"><div class="stat-number">Free</div><div class="stat-label">Sample</div></div>
            </div>
        </div>
    </div>
    <div class="scroll-hint">SCROLL<div class="scroll-line"></div></div>
</section>

<!-- Problem -->
<section class="bg-secondary" style="background:#130F1F" id="problem">
    <div class="container">
        <div class="two-col-intro" style="display:grid; grid-template-columns:1fr 1fr; gap:2rem; margin-bottom:3rem">
            <h2 class="rv-left heading-font" style="font-size:clamp(2rem,3vw,2.6rem)">Traditional photoshoots / cost you time, money, / and momentum.</h2>
            <p class="rv-right" style="color:var(--text-muted)">Booking models, studios, and retouching delays your launch. VELOX removes every obstacle.</p>
        </div>
        <div class="problem-grid">
            <div class="p-card"><div class="card-number">01</div><h3>Model booking delays</h3><p style="font-size:0.85rem">No availability? No problem.</p></div>
            <div class="p-card"><div class="card-number">02</div><h3>Venue & studio costs</h3><p>Save thousands per shoot.</p></div>
            <div class="p-card"><div class="card-number">03</div><h3>Slow editing turnaround</h3><p>48h delivery standard.</p></div>
            <div class="p-card"><div class="card-number">04</div><h3>Inconsistent brand look</h3><p>Unified aesthetic every time.</p></div>
        </div>
    </div>
</section>

<!-- Services -->
<section id="services" style="background:var(--bg-primary)">
    <div class="container">
        <span class="section-label">One service. Every fashion category.</span>
        <div class="services-grid" style="margin-top:1.5rem">
            <div class="svc-card"><svg width="36" height="36" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1"> <path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"/></svg><h3>Apparel & Clothing</h3><p>Salwar, western, kids, formal</p></div>
            <div class="svc-card"><svg width="36" height="36" viewBox="0 0 24 24" fill="none" stroke="currentColor"><rect x="2" y="7" width="20" height="14" rx="2"/><path d="M16 21V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v16"/></svg><h3>Bags & Accessories</h3><p>Premium lifestyle imagery</p></div>
            <div class="svc-card"><svg width="36" height="36" viewBox="0 0 24 24" fill="none" stroke="currentColor"><circle cx="12" cy="12" r="2"/><path d="M12 2a15 15 0 0 0 0 20 15 15 0 0 0 0-20z"/></svg><h3>Watches & Jewellery</h3><p>Detail shots, elegance</p></div>
            <div class="svc-card"><svg width="36" height="36" viewBox="0 0 24 24" fill="none" stroke="currentColor"><path d="M18 4v16M6 4v16M4 8h16M4 16h16"/></svg><h3>Footwear</h3><p>Sneakers, heels, formal</p></div>
            <div class="svc-card"><svg width="36" height="36" viewBox="0 0 24 24" fill="none" stroke="currentColor"><circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/></svg><h3>Sunglasses & Eyewear</h3><p>On-model editorial look</p></div>
            <div class="svc-card"><svg width="36" height="36" viewBox="0 0 24 24" fill="none" stroke="currentColor"><path d="M12 2v4M22 12h-4M6 12H2M12 22v-4"/><circle cx="12" cy="12" r="3"/></svg><h3>Perfume & Beauty</h3><p>Brand campaigns, cosmetics</p></div>
        </div>
    </div>
</section>

<!-- Showcase with Tabs -->
<section id="showcase" style="background:var(--bg-tertiary)">
    <div class="container">
        <div style="display:grid; grid-template-columns:1fr 1fr; gap:2rem; margin-bottom:2rem">
            <h2 class="heading-font">Our model portfolio.<br>Your visual reference.</h2>
            <p class="rv-right">Explore the variety of AI fashion models available.</p>
        </div>
        <div class="showcase-tabs">
            <button class="tab-btn active" data-tab="tab1">Model styles</button>
            <button class="tab-btn" data-tab="tab2">By category</button>
            <button class="tab-btn" data-tab="tab3">Client samples</button>
        </div>
        <!-- Tab 1: 24 slots -->
        <div id="tab1" class="tab-content active-tab">
            <div class="model-grid grid-6x4" id="modelStylesGrid"></div>
        </div>
        <div id="tab2" class="tab-content">
            <div class="model-grid grid-6x2" id="categoriesGrid"></div>
        </div>
        <div id="tab3" class="tab-content">
            <div class="model-grid grid-4x2" id="samplesGrid"></div>
        </div>
    </div>
</section>

<!-- Process -->
<section id="process" style="background:var(--bg-secondary)">
    <div class="container process-wrapper">
        <div class="process-sticky"><h2 class="heading-font">Three steps.<br>48–72 hours.</h2><p class="rv-left" style="margin-top:1rem">From product photo to pro model image.</p></div>
        <div class="process-steps">
            <div class="step-row"><div class="card-number">01</div><h3>Send your product photo</h3><p>Any smartphone image works</p></div>
            <div class="step-row"><div class="card-number">02</div><h3>Share your creative brief</h3><p>Model type, background, pose</p></div>
            <div class="step-row"><div class="card-number">03</div><h3>Receive catalog-ready visuals</h3><p>Print & social media ready</p></div>
            <div class="step-row"><div class="card-number">04</div><h3>Revisions until perfect</h3><p>Fine-tune with your team</p></div>
        </div>
    </div>
</section>

<!-- Offerings -->
<section id="offerings" style="background:var(--bg-primary)">
    <div class="container">
        <h2 class="heading-font rv">Everything in fashion, <br class="hide-mob">except saree.</h2>
        <div class="offerings-list" style="margin-top:2rem">
            <div class="offering-item"><span>Salwar Kameez</span><span style="color:var(--gold)">●</span></div>
            <div class="offering-item"><span>Western Wear</span><span style="color:var(--gold)">●</span></div>
            <div class="offering-item"><span>Kids Wear</span><span style="color:var(--gold)">●</span></div>
            <div class="offering-item"><span>Formal & Office</span><span style="color:var(--gold)">●</span></div>
            <div class="offering-item"><span>Footwear</span><span style="color:var(--gold)">●</span></div>
            <div class="offering-item"><span>Sunglasses & Eyewear</span><span style="color:var(--gold)">●</span></div>
            <div class="offering-item"><span>Watches & Jewellery</span><span style="color:var(--gold)">●</span></div>
            <div class="offering-item"><span>Bags & Accessories</span><span style="color:var(--gold)">●</span></div>
            <div class="offering-item"><span>Perfume & Beauty</span><span style="color:var(--gold)">●</span></div>
            <div class="offering-item"><span>Casual & Streetwear</span><span style="color:var(--gold)">●</span></div>
        </div>
        <div style="border:1px solid var(--gold); padding:1.2rem; margin-top:2rem; text-align:center">
            ℹ️ Saree photography is a specialty we reserve for our upcoming premium studio collection. For now, all other categories are supported.
        </div>
    </div>
</section>

<!-- Why Choose Us -->
<section style="background:var(--bg-tertiary)">
    <div class="container">
        <h2 class="heading-font">The advantage of<br>going AI-first.</h2>
        <div class="why-grid" style="grid-template-columns:repeat(3,1fr); margin-top:2rem">
            <div class="why-card"><div class="stat-number">48h</div><h3>Fast turnaround</h3><p>Express delivery</p></div>
            <div class="why-card"><div class="stat-number">∞</div><h3>No scheduling limits</h3><p>Any time, any volume</p></div>
            <div class="why-card"><div class="stat-number">You</div><h3>Full creative control</h3><p>Direct every detail</p></div>
            <div class="why-card"><div class="stat-number">1/5</div><h3>Fraction of cost</h3><p>Save up to 80%</p></div>
            <div class="why-card"><div class="stat-number">2in1</div><h3>Print & digital ready</h3><p>High-res optimized</p></div>
            <div class="why-card"><div class="stat-number">✓</div><h3>Consistent brand look</h3><p>Unified AI aesthetics</p></div>
        </div>
    </div>
</section>

<!-- CTA Light -->
<section class="cta-light">
    <div class="container" style="display:grid; grid-template-columns:1fr 1fr; gap:2rem">
        <div><h2 class="heading-font" style="color:#0A0715">See it for yourself<br>before you commit.</h2><div style="margin:1rem 0">✓ Free sample image<br>✓ No hidden changes<br>✓ 48–72h delivery<br>✓ You choose everything<br>✓ Social & print ready</div></div>
        <div class="cta-card"><div style="color:var(--gold)">✦ Free sample offer</div><h3 style="color:#FCFAF7; margin:1rem 0">Send a photo. Get your catalog image back.</h3><a href="https://wa.me/8801790330221?text=Hi%2C%20I'd%20like%20a%20free%20sample%20image%20for%20my%20product" target="_blank" class="btn-sample" style="background:var(--gold); width:100%; text-align:center; display:block">Claim Free Sample →</a><p style="color:#A99FBF; font-size:0.7rem; margin-top:1rem">Mahir Daiyan Siyam · 01790330221 · Reply within 24h</p></div>
    </div>
</section>

<footer><div class="container"><div class="logo">VELOX<span>.</span></div><p style="margin:1rem 0">© 2025 VELOX. All rights reserved. AI model photography studio, Bangladesh.</p><a href="https://wa.me/8801790330221" target="_blank" style="color:var(--gold)">WhatsApp +8801790330221</a></div></footer>

<script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
<script>
    // --- 3D ROYALTY SCENE ---
    const scene = new THREE.Scene();
    scene.background = new THREE.Color(0x0A0715);
    const camera = new THREE.PerspectiveCamera(45, window.innerWidth / window.innerHeight, 0.1, 1000);
    camera.position.set(0, 0, 12);
    const renderer = new THREE.WebGLRenderer({ alpha: false, antialias: true });
    renderer.setSize(window.innerWidth, window.innerHeight);
    renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
    renderer.setClearColor(0x0A0715, 1);
    const canvasElem = renderer.domElement;
    canvasElem.style.position = 'fixed';
    canvasElem.style.top = '0';
    canvasElem.style.left = '0';
    canvasElem.style.zIndex = '0';
    canvasElem.style.pointerEvents = 'none';
    document.body.insertBefore(canvasElem, document.body.firstChild);

    // Gold torus knot
    const geometry = new THREE.TorusKnotGeometry(1.8, 0.3, 200, 32, 3, 4);
    const material = new THREE.MeshStandardMaterial({ color: 0xD4AF37, emissive: 0x3D2B63, emissiveIntensity: 0.5, metalness: 0.8, roughness: 0.3, wireframe: false });
    const knot = new THREE.Mesh(geometry, material);
    scene.add(knot);
    
    // Particles
    const particleCount = 180;
    const particlesGeometry = new THREE.BufferGeometry();
    const particlePositions = new Float32Array(particleCount * 3);
    for (let i = 0; i < particleCount; i++) {
        particlePositions[i*3] = (Math.random() - 0.5) * 20;
        particlePositions[i*3+1] = (Math.random() - 0.5) * 12;
        particlePositions[i*3+2] = (Math.random() - 0.5) * 15 - 5;
    }
    particlesGeometry.setAttribute('position', new THREE.BufferAttribute(particlePositions, 3));
    const particleMaterial = new THREE.PointsMaterial({ color: 0xD4AF37, size: 0.08, transparent: true, opacity: 0.5 });
    const particleSystem = new THREE.Points(particlesGeometry, particleMaterial);
    scene.add(particleSystem);
    
    const ambientLight = new THREE.AmbientLight(0x2A1B4D);
    scene.add(ambientLight);
    const dirLight = new THREE.DirectionalLight(0xffe6aa, 1);
    dirLight.position.set(2,5,3);
    scene.add(dirLight);
    const backLight = new THREE.PointLight(0x8a6eff, 0.4);
    backLight.position.set(-2,1,-4);
    scene.add(backLight);
    
    let mouseX = 0, mouseY = 0;
    document.addEventListener('mousemove', (e) => {
        mouseX = (e.clientX / window.innerWidth) * 2 - 1;
        mouseY = (e.clientY / window.innerHeight) * 2 - 1;
    });
    
    function animate() {
        requestAnimationFrame(animate);
        knot.rotation.x += 0.003;
        knot.rotation.y += 0.005;
        particleSystem.rotation.y += 0.0005;
        particleSystem.rotation.x += 0.0003;
        camera.position.x += (mouseX * 0.5 - camera.position.x) * 0.03;
        camera.position.y += (-mouseY * 0.3 - camera.position.y) * 0.03;
        camera.lookAt(0, 0, 0);
        renderer.render(scene, camera);
    }
    animate();
    window.addEventListener('resize', () => {
        camera.aspect = window.innerWidth / window.innerHeight;
        camera.updateProjectionMatrix();
        renderer.setSize(window.innerWidth, window.innerHeight);
    });
    
    // --- populate placeholder slots ---
    const categories = ["Ethnic—Female", "Western—Female", "Casual—Female", "Formal—Female", "Editorial—Female", "Luxury—Female", "Ethnic—Male", "Western—Male", "Casual—Male", "Formal—Male", "Streetwear—Male", "Editorial—Male", "Kids—Girl", "Kids—Boy", "Studio—White BG", "Outdoor—Urban", "Outdoor—Nature", "Luxury Interior", "Accessories Close-up", "Footwear—On Foot", "Watch—Wrist Shot", "Perfume—Lifestyle", "Sunglasses—Editorial", "Bag—Campaign"];
    const modelGrid = document.getElementById("modelStylesGrid");
    for(let i=0; i<24; i++) {
        let div = document.createElement('div'); div.className = "slot";
        div.innerHTML = `<div class="slot-number">${String(i+1).padStart(2,'0')}</div><span class="coming">Coming Soon</span><div class="slot-label">${categories[i]}</div>`;
        modelGrid.appendChild(div);
    }
    const prodCategories = ["Salwar Kameez","Western Wear","Kids Wear","Formal","Footwear","Sunglasses","Watches","Jewellery","Bags","Perfume","Beauty","Casual"];
    const catGrid = document.getElementById("categoriesGrid");
    for(let i=0; i<12; i++) {
        let div = document.createElement('div'); div.className = "slot"; div.style.aspectRatio="1/1";
        div.innerHTML = `<span class="coming">${prodCategories[i]}</span><div style="margin-top:8px; font-size:0.6rem">Coming Soon</div>`;
        catGrid.appendChild(div);
    }
    const sampleGrid = document.getElementById("samplesGrid");
    for(let i=1; i<=8; i++) {
        let div = document.createElement('div'); div.className = "sample-slot";
        div.innerHTML = `<div class="sample-badge">Sample 0${i}</div><span class="coming">Coming Soon</span>`;
        sampleGrid.appendChild(div);
    }
    
    // Tabs logic
    const tabs = document.querySelectorAll('.tab-btn');
    const contents = document.querySelectorAll('.tab-content');
    tabs.forEach(btn => {
        btn.addEventListener('click', () => {
            const tabId = btn.getAttribute('data-tab');
            tabs.forEach(b => b.classList.remove('active'));
            btn.classList.add('active');
            contents.forEach(c => c.classList.remove('active-tab'));
            document.getElementById(tabId).classList.add('active-tab');
            // re-trigger observer on new visible elements
            observeAll();
        });
    });
    
    // IntersectionObserver (single, re-trigger)
    const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if(entry.isIntersecting) entry.target.classList.add('in');
            else entry.target.classList.remove('in');
        });
    }, { threshold: 0.1 });
    function observeAll() {
        document.querySelectorAll('.rv, .rv-left, .rv-right, .hero-tag, .hero-headline, .hero-bottom').forEach(el => observer.observe(el));
        document.querySelectorAll('.svc-card, .p-card, .why-card, .step-row, .offering-item').forEach(el => observer.observe(el));
        document.querySelectorAll('.rv, .rv-left, .rv-right').forEach(el => observer.observe(el));
    }
    observeAll();
    
    // Custom cursor (desktop)
    if(window.innerWidth > 900) {
        const cursorDot = document.createElement('div'); cursorDot.className = 'custom-cursor'; document.body.appendChild(cursorDot);
        const cursorRing = document.createElement('div'); cursorRing.className = 'cursor-ring'; document.body.appendChild(cursorRing);
        let mouse = { x: 0, y: 0 }, ring = { x: 0, y: 0 };
        document.addEventListener('mousemove', (e) => { mouse.x = e.clientX; mouse.y = e.clientY; cursorDot.style.transform = `translate(${mouse.x-6}px, ${mouse.y-6}px)`; });
        function lerpRing() { ring.x += (mouse.x - ring.x) * 0.12; ring.y += (mouse.y - ring.y) * 0.12; cursorRing.style.transform = `translate(${ring.x-20}px, ${ring.y-20}px)`; requestAnimationFrame(lerpRing); } lerpRing();
        const interactive = document.querySelectorAll('a, button, .btn-sample, .tab-btn');
        interactive.forEach(el => { el.addEventListener('mouseenter', () => { cursorDot.style.width = '20px'; cursorDot.style.height = '20px'; cursorRing.style.width = '60px'; cursorRing.style.height = '60px'; }); el.addEventListener('mouseleave', () => { cursorDot.style.width = '12px'; cursorDot.style.height = '12px'; cursorRing.style.width = '40px'; cursorRing.style.height = '40px'; }); });
    }
    // Nav shrink
    window.addEventListener('scroll', () => { const nav = document.getElementById('navbar'); if(window.scrollY > 80) nav.classList.add('shrink'); else nav.classList.remove('shrink'); });
    // Card tilt effect (simple for perf)
    document.querySelectorAll('.p-card, .svc-card').forEach(card => {
        card.addEventListener('mousemove', (e) => { const rect = card.getBoundingClientRect(); const x = (e.clientX - rect.left) / rect.width - 0.5; const y = (e.clientY - rect.top) / rect.height - 0.5; card.style.transform = `perspective(700px) rotateY(${x*12}deg) rotateX(${-y*12}deg) scale(1.02)`; });
        card.addEventListener('mouseleave', () => { card.style.transform = 'perspective(700px) rotateY(0deg) rotateX(0deg) scale(1)'; });
    });
</script>
</body>
</html>